# Eureka-Moments in Transformers: Multi-Step Tasks Reveal Softmax Induced Optimization Problems

**Hoffmann, Schrodi, Bratulić, Behrmann, Fischer & Brox — "Eureka-Moments in Transformers: Multi-Step Tasks Reveal Softmax Induced Optimization Problems"** · arXiv:2310.12956 (v2) · serialisation: the native arXiv HTML, converted with page marks against the arXiv PDF (task-008, 2026-08).

**An external work; the abstract is given.** The subject of the paper is the abrupt jumps of the training loss on multi-step tasks ("Eureka moments") and their cause in the softmax of self-attention. The paper itself separates its phenomenon from grokking: the plateau here comes before the task is solved at all, and there is no train/val gap; for the corpus this is a valuable negative control for theories of regularisation. Set aside by the triage of task-021 (2026-08-26). No work of the corpus cites it yet; it is cited by the external work 2506.13688↗ (set aside by the same triage). The introduction and the quoted paragraphs are translated lazily, as quotations appear (decision D2-b). The original — [`original/`](https://arxiv.org/abs/2310.12956); the analysis of the claims is the neighbouring `…claims.txt`.

**Excerpt card.** The paper's licence (`arxiv-nonexclusive`) does not permit republishing the paper itself; what stands here are the editorial sections and the abstract (right of quotation). The paper: <https://arxiv.org/abs/2310.12956>.

# Abstract

*"In this work, we study rapid improvements of the training loss in transformers when being confronted with multi-step decision tasks. We found that transformers struggle to learn the intermediate task and both training and validation loss saturate for hundreds of epochs. When transformers finally learn the intermediate task, they do this rapidly and unexpectedly. We call these abrupt improvements *Eureka-moments*, since the transformer appears to suddenly learn a previously incomprehensible concept. We designed synthetic tasks to study the problem in detail, but the leaps in performance can be observed also for language modeling and in-context learning (ICL). We suspect that these abrupt transitions are caused by the multi-step nature of these tasks. Indeed, we find connections and show that ways to improve on the synthetic multi-step tasks can be used to improve the training of language modeling and ICL. Using the synthetic data we trace the problem back to the Softmax function in the self-attention block of transformers and show ways to alleviate the problem. These fixes reduce the required number of training steps, lead to higher likelihood to learn the intermediate task, to higher final accuracy and training becomes more robust to hyper-parameters."*
