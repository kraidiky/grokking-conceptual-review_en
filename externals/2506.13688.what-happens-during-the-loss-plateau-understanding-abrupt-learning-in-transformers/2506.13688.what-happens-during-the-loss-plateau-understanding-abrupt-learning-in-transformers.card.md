# What Happens During the Loss Plateau? Understanding Abrupt Learning in Transformers

**Gopalani & Hu — "What Happens During the Loss Plateau? Understanding Abrupt Learning in Transformers"** · arXiv:2506.13688 (v2) · serialisation: the native arXiv HTML, converted with page marks against the arXiv PDF (task-008, 2026-08).

**An external work; the abstract is given.** The subject of the paper is abrupt learning on algorithmic tasks: a partial solution, a repetition bias and a collapse of the representations during the plateau, with the slow learning of the attention maps as the bottleneck. The paper itself separates its subject from grokking (the plateau comes before the task is solved at all, and there is no train/test gap); what the corpus values here are signs of the plateau computable without knowing the solution. Set aside by the triage of task-021 (2026-08-26; the introduction was read under the escalation ladder). No work of the corpus cites it yet. The introduction and the quoted paragraphs are translated lazily, as quotations appear (decision D2-b). The original — [`original/`](https://arxiv.org/abs/2506.13688); the analysis of the claims is the neighbouring `…claims.txt`.

**Excerpt card.** The paper's licence (`arxiv-nonexclusive`) does not permit republishing the paper itself; what stands here are the editorial sections and the abstract (right of quotation). The paper: <https://arxiv.org/abs/2506.13688>.

# Abstract

*"Training Transformers on algorithmic tasks frequently demonstrates an intriguing *abrupt learning* phenomenon: an extended performance plateau followed by a sudden, sharp improvement. This work investigates the underlying mechanisms for such dynamics, primarily in shallow Transformers. We reveal that during the plateau, the model often develops an interpretable *partial solution* while simultaneously exhibiting a strong *repetition bias* in their outputs. This output degeneracy is accompanied by *internal representation collapse*, where hidden states across different tokens become nearly parallel. We further identify the slow learning of optimal attention maps as a key bottleneck. Hidden progress in attention configuration during the plateau precedes the eventual rapid convergence, and directly intervening on attention significantly alters plateau duration and the severity of repetition bias and representational collapse. We validate that these identified phenomena—repetition bias and representation collapse—are not artifacts of toy setups but also manifest in the early pre-training stage of large language models like Pythia and OLMo."*
