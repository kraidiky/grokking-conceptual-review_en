# A Theory of Generalization in Deep Learning

**Litman & Guo — "A Theory of Generalization in Deep Learning"** · arXiv:2605.01172 (v1) · serialisation: the native arXiv HTML, converted with page marks against the arXiv PDF (task-008, 2026-08).

**An external work; the abstract is given.** The subject of the paper is a non-asymptotic theory of generalisation in which the empirical NTK partitions the output space into a signal channel and a noise reservoir. Grokking is one of four phenomena brought under the theory; what the corpus values here are the criterion "signal against memorisation" and the SNR preconditioner (a claimed fivefold acceleration of grokking). Set aside by the triage of task-021 (2026-08-26). No work of the corpus cites it yet. The introduction and the quoted paragraphs are translated lazily, as quotations appear (decision D2-b). The original — [`original/`](original/2605.01172.a-theory-of-generalization-in-deep-learning.md); the analysis of the claims is the neighbouring `…claims.txt`.

# Abstract

<i>"We present a non-asymptotic theory of generalization in deep learning where the empirical neural tangent kernel partitions the output space. In directions corresponding to signal, error dissipates rapidly; in the vast orthogonal dimensions corresponding to noise, the kernel’s near-zero eigenvalues trap residual error in a test-invisible reservoir. Within the signal channel, minibatch SGD ensures that coherent population signal accumulates via fast linear drift, while idiosyncratic memorization is suppressed into a slow, diffusive random walk. We prove generalization survives even when the kernel evolves $\mathcal{O}(1)$ in operator norm, the full feature-learning regime. This theory naturally explains disparate phenomena in deep learning theory, such as benign overfitting, double descent, implicit bias, and grokking. Lastly, we derive an exact population-risk objective from a single training run with no validation data, for any architecture, loss, or optimizer, and prove that it measures precisely the noise in the signal channel. This objective reduces in practice to an SNR preconditioner on top of Adam, adding one state vector at no extra cost; it accelerates grokking by $5\times$, suppresses memorization in PINNs and implicit neural representations, and improves DPO fine-tuning under noisy preferences while staying $3\times$ closer to the reference policy."</i>

# Paper text

The full text of the paper is not reproduced here. It is stored, as the
authors published it, in [`original/2605.01172.a-theory-of-generalization-in-deep-learning.md`](original/2605.01172.a-theory-of-generalization-in-deep-learning.md); every
anchor referenced above points into that file.
