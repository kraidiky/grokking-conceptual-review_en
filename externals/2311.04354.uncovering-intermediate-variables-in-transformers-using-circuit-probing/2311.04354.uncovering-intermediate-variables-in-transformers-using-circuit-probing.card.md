# Uncovering Intermediate Variables in Transformers using Circuit Probing

**Lepori, Serre & Pavlick — "Uncovering Intermediate Variables in Transformers using Circuit Probing"** · arXiv:2311.04354 (v3) · serialisation: the native arXiv HTML, converted with page marks against the arXiv PDF (task-008, 2026-08).

**An external work; the abstract is given.** The subject of the paper is the interpretability instrument circuit probing: an automatic search for low-level circuits that compute hypothesised intermediate variables. Grokking is one of four applications (tracking the formation of circuits over training); what the corpus values here is a progress measure that requires no reverse engineering, and the demonstrated unsuitability of ordinary probes. Set aside by the triage of task-021 (2026-08-26). No work of the corpus cites it yet; it is cited by the external work 2508.15841↗ (set aside by the same triage). The introduction and the quoted paragraphs are translated lazily, as quotations appear (decision D2-b). The original — [`original/`](original/2311.04354.uncovering-intermediate-variables-in-transformers-using-circuit-probing.md); the analysis of the claims is the neighbouring `…claims.txt`.

# Abstract

*"Neural network models have achieved high performance on a wide variety of complex tasks, but the algorithms that they implement are notoriously difficult to interpret. It is often necessary to hypothesize intermediate variables involved in a network’s computation in order to understand these algorithms. For example, does a language model depend on particular syntactic properties when generating a sentence? Yet, existing analysis tools make it difficult to test hypotheses of this type. We propose a new analysis technique – *circuit probing* – that automatically uncovers low-level circuits that compute hypothesized intermediate variables. This enables causal analysis through targeted ablation at the level of model parameters. We apply this method to models trained on simple arithmetic tasks, demonstrating its effectiveness at (1) deciphering the algorithms that models have learned, (2) revealing modular structure within a model, and (3) tracking the development of circuits over training. Across these three experiments we demonstrate that circuit probing combines and extends the capabilities of existing methods, providing one unified approach for a variety of analyses. Finally, we demonstrate circuit probing on a real-world use case: uncovering circuits that are responsible for subject-verb agreement and reflexive anaphora in GPT2-Small and Medium."*

# Paper text

The full text of the paper is not reproduced here. It is stored, as the
authors published it, in [`original/2311.04354.uncovering-intermediate-variables-in-transformers-using-circuit-probing.md`](original/2311.04354.uncovering-intermediate-variables-in-transformers-using-circuit-probing.md); every
anchor referenced above points into that file.
