# The Weight Gram Matrix Captures Sequential Feature Linearization in Deep Networks

**Cha, Beaglehole, Radhakrishnan & Lee — "The Weight Gram Matrix Captures Sequential Feature Linearization in Deep Networks"** · arXiv:2605.06258 (v1) · serialisation: the native arXiv HTML, converted with page marks against the arXiv PDF (task-008, 2026-08).

**An external work; the abstract is given.** The subject of the paper is a feature-centric frame of training through the weight Gram matrix (the Feature Learning Equation, the Virtual Covariance, Target Linearity). The grokking runs are validating ones; what the corpus values here is a cheap layer-wise measure computed on the training sample. Set aside by the triage of task-021 (2026-08-26). No work of the corpus cites it yet. The introduction and the quoted paragraphs are translated lazily, as quotations appear (decision D2-b). The original — [`original/`](original/2605.06258.the-weight-gram-matrix-captures-sequential-feature-linearization-in-deep-networks.md); the analysis of the claims is the neighbouring `…claims.txt`.

# Abstract

*"Understanding how deep neural networks learn representations remains a central challenge in machine learning theory. In this work, we propose a feature-centric framework for analyzing neural network training by relating weight updates to feature evolution. We introduce a simple identity, the *Feature Learning Equation*, which identifies the weight Gram matrix as the key object capturing feature dynamics. This enables us to interpret gradient descent as implicitly inducing a hypothetical evolution of features, whose covariance structure — termed the *Virtual Covariance* — characterizes how representations evolve during training. Building on this perspective, we introduce *Target Linearity*, a measure quantifying the linear alignment between features and targets. By analyzing the training and layer-wise dynamics, we show that deep networks learn to *sequentially* transform representations toward target-linear structure. This linearization perspective provides a unified interpretation of several empirical phenomena, including Neural Collapse and linear interpolation in generative models. Code is available at https://github.com/cth127/GramLin."*

# Paper text

The full text of the paper is not reproduced here. It is stored, as the
authors published it, in [`original/2605.06258.the-weight-gram-matrix-captures-sequential-feature-linearization-in-deep-networks.md`](original/2605.06258.the-weight-gram-matrix-captures-sequential-feature-linearization-in-deep-networks.md); every
anchor referenced above points into that file.
